# Какие файлы загрузить на сервер

## ✅ Файлы, которые НУЖНО загрузить:

### 📁 Структура проекта:

```
ваш-проект/
├── app/                          # ✅ ВСЯ папка app/
│   ├── admin/
│   ├── api/
│   ├── components/
│   ├── globals.css
│   ├── layout.tsx
│   ├── page.css
│   └── page.tsx
│
├── lib/                          # ✅ ВСЯ папка lib/
│   ├── prisma.ts
│   └── whatsapp.ts
│
├── prisma/                       # ✅ Папка prisma/ (БЕЗ dev.db!)
│   └── schema.prisma             # ✅ Только schema.prisma
│
├── package.json                  # ✅ ОБЯЗАТЕЛЬНО
├── package-lock.json             # ✅ ОБЯЗАТЕЛЬНО (или yarn.lock)
├── tsconfig.json                 # ✅ ОБЯЗАТЕЛЬНО
├── next.config.js                # ✅ ОБЯЗАТЕЛЬНО
├── tailwind.config.js            # ✅ ОБЯЗАТЕЛЬНО
├── postcss.config.js             # ✅ ОБЯЗАТЕЛЬНО
├── vercel.json                   # ✅ Если используете Vercel
├── .gitignore                    # ✅ Полезно иметь
│
└── README.md                     # ⚠️ Опционально (можно не загружать)
    DEPLOYMENT.md                 # ⚠️ Опционально (можно не загружать)
    SETUP.md                      # ⚠️ Опционально (можно не загружать)
```

---

## ❌ Файлы, которые НЕ НУЖНО загружать:

### 🚫 Исключить:

```
❌ node_modules/          # Устанавливается на сервере через npm install
❌ .next/                 # Создается при сборке (npm run build)
❌ .env                   # Создается на сервере с вашими данными
❌ .env.local             # Локальные переменные
❌ prisma/dev.db          # Локальная база данных (не нужна!)
❌ prisma/dev.db-journal  # Локальная база данных
❌ .vercel/               # Папка Vercel (если есть)
❌ *.log                  # Логи
❌ .DS_Store              # Системные файлы Mac
❌ Thumbs.db              # Системные файлы Windows
```

---

## 📋 Пошаговая инструкция:

### Вариант 1: Через Git (Рекомендуется)

1. **Инициализируйте Git репозиторий:**
```bash
git init
git add .
git commit -m "Initial commit"
```

2. **Создайте репозиторий на GitHub/GitLab**

3. **Загрузите код:**
```bash
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

4. **На сервере клонируйте:**
```bash
git clone https://github.com/ваш-username/ваш-репозиторий.git
cd ваш-репозиторий
```

---

### Вариант 2: Через FTP/SFTP

**Создайте архив с нужными файлами:**

#### Windows (PowerShell):
```powershell
# Создайте архив, исключая ненужные файлы
Compress-Archive -Path app,lib,prisma\schema.prisma,package.json,package-lock.json,tsconfig.json,next.config.js,tailwind.config.js,postcss.config.js,vercel.json,.gitignore -DestinationPath project.zip -Force
```

#### Linux/Mac:
```bash
# Создайте архив
tar -czf project.tar.gz \
  app/ \
  lib/ \
  prisma/schema.prisma \
  package.json \
  package-lock.json \
  tsconfig.json \
  next.config.js \
  tailwind.config.js \
  postcss.config.js \
  vercel.json \
  .gitignore
```

**Загрузите архив на сервер и распакуйте:**
```bash
unzip project.zip
# или
tar -xzf project.tar.gz
```

---

## 🔧 После загрузки на сервер:

### 1. Установите зависимости:
```bash
npm install --production
```

### 2. Создайте файл `.env`:
```bash
nano .env
```

Добавьте:
```
DATABASE_URL="postgresql://user:password@host:5432/dbname"
NODE_ENV="production"
```

### 3. Сгенерируйте Prisma Client:
```bash
npx prisma generate
```

### 4. Примените миграции базы данных:
```bash
npx prisma migrate deploy
# или если используете db push:
npx prisma db push
```

### 5. Соберите проект:
```bash
npm run build
```

### 6. Запустите:
```bash
npm start
```

---

## 📦 Минимальный набор файлов (список):

Если загружаете вручную, убедитесь, что у вас есть:

- ✅ `app/` (вся папка)
- ✅ `lib/` (вся папка)
- ✅ `prisma/schema.prisma`
- ✅ `package.json`
- ✅ `package-lock.json`
- ✅ `tsconfig.json`
- ✅ `next.config.js`
- ✅ `tailwind.config.js`
- ✅ `postcss.config.js`
- ✅ `vercel.json` (если используете Vercel)

**Всего: ~50-100 файлов** (в зависимости от размера проекта)

---

## ⚠️ Важные замечания:

1. **НЕ загружайте `node_modules`** - это займет много места и времени. Установите на сервере через `npm install`

2. **НЕ загружайте `.env`** - создайте новый на сервере с правильными данными для продакшена

3. **НЕ загружайте базу данных SQLite** (`dev.db`) - для продакшена используйте PostgreSQL

4. **НЕ загружайте `.next`** - эта папка создается при сборке проекта

5. **Проверьте `.gitignore`** - он показывает, что не нужно загружать

---

## 🚀 Быстрая проверка перед загрузкой:

Перед загрузкой убедитесь, что у вас есть:
- [ ] Папка `app/` со всеми файлами
- [ ] Папка `lib/` со всеми файлами
- [ ] Файл `prisma/schema.prisma`
- [ ] Файл `package.json`
- [ ] Файл `package-lock.json` (или `yarn.lock`)
- [ ] Файл `tsconfig.json`
- [ ] Файл `next.config.js`
- [ ] Файл `tailwind.config.js`
- [ ] Файл `postcss.config.js`

И убедитесь, что НЕТ:
- [ ] Папки `node_modules/`
- [ ] Папки `.next/`
- [ ] Файла `.env`
- [ ] Файла `prisma/dev.db`


