# Сайт для записи на сеанс мануальной терапии

Веб-приложение для онлайн-записи клиентов на сеансы мануальной терапии.

## Возможности

- 📅 Календарь для выбора даты
- ⏰ Выбор времени из доступных слотов
- 📝 Форма записи с контактными данными
- 👨‍💼 Админ-панель для управления записями
- 🔄 Управление статусами записей
- 📊 Фильтрация записей по статусам

## Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Prisma** - ORM для работы с базой данных
- **SQLite** - База данных
- **Tailwind CSS** - Стилизация
- **React Calendar** - Компонент календаря

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Настройте базу данных:
```bash
npx prisma generate
npx prisma db push
```

3. Запустите сервер разработки:
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Использование

### Главная страница
- Выберите дату в календаре
- Выберите доступное время
- Заполните форму с контактными данными
- Отправьте запись

### Админ-панель
Откройте `/admin` для управления записями:
- Просмотр всех записей
- Изменение статусов записей
- Удаление записей
- Фильтрация по статусам

## Развертывание

См. подробную инструкцию в [DEPLOYMENT.md](./DEPLOYMENT.md)

### Быстрый старт на Vercel:

1. Установите Vercel CLI: `npm i -g vercel`
2. Войдите: `vercel login`
3. Разверните: `vercel`
4. Настройте переменные окружения в Vercel Dashboard
5. Напоминания будут отправляться автоматически через Vercel Cron Jobs

**Важно:** Для продакшена используйте PostgreSQL вместо SQLite!

## Структура проекта

```
├── app/
│   ├── api/
│   │   ├── appointments/     # API endpoints для записей
│   │   ├── reminders/         # API для отправки напоминаний
│   │   └── whatsapp/          # Webhook для WhatsApp
│   ├── admin/                 # Админ-панель
│   ├── globals.css            # Глобальные стили
│   ├── layout.tsx             # Корневой layout
│   └── page.tsx               # Главная страница
├── lib/
│   └── prisma.ts              # Prisma client
├── prisma/
│   └── schema.prisma          # Схема базы данных
└── package.json
```

## База данных

Модель `Appointment` содержит:
- Имя клиента
- Телефон
- Email (опционально)
- Дата и время
- Заметки (опционально)
- Статус (pending, confirmed, cancelled, completed)

## Разработка

Для работы с базой данных используйте Prisma Studio:
```bash
npm run db:studio
```



