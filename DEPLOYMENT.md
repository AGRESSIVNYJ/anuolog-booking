# Инструкция по развертыванию

> 💡 **Если у вас есть домен** - см. подробную инструкцию в [DOMAIN_SETUP.md](./DOMAIN_SETUP.md)

## Варианты хостинга

### ✅ Вариант 1: Vercel (Рекомендуется)

**Преимущества:**
- ✅ **Полностью бесплатный** для вашего проекта (см. [VERCEL_PRICING.md](./VERCEL_PRICING.md))
- Бесплатный хостинг для Next.js
- Автоматическое развертывание
- Встроенные Cron Jobs для автоматической отправки напоминаний
- HTTPS из коробки
- Публичный URL для webhook
- 1,000,000 вызовов API в месяц (более чем достаточно!)

**Шаги:**

1. **Установите Vercel CLI:**
```bash
npm i -g vercel
```

2. **Войдите в Vercel:**
```bash
vercel login
```

3. **Разверните проект:**
```bash
vercel
```

4. **Настройте переменные окружения в Vercel Dashboard:**
   - `DATABASE_URL` - для продакшена используйте PostgreSQL или другую БД
   - Другие переменные при необходимости

5. **Настройте Cron Job для напоминаний:**
   - Создайте файл `vercel.json` (см. ниже)
   - Или используйте Vercel Cron Jobs в настройках проекта

**Файл `vercel.json` для автоматических напоминаний:**
```json
{
  "crons": [
    {
      "path": "/api/reminders/send",
      "schedule": "*/15 * * * *"
    }
  ]
}
```
Это будет запускать проверку напоминаний каждые 15 минут.

---

### ✅ Вариант 2: Обычный хостинг (Shared Hosting)

**Требования:**
- Node.js поддержка
- Возможность запускать cron jobs
- Публичный URL

**Шаги:**

1. Загрузите файлы на хостинг
2. Установите зависимости: `npm install --production`
3. Соберите проект: `npm run build`
4. Настройте cron job для автоматической отправки напоминаний

**Cron job команда (каждые 15 минут):**
```bash
*/15 * * * * curl -X POST https://ваш-домен.com/api/reminders/send
```

---

### ✅ Вариант 3: VPS/Сервер

**Преимущества:**
- Полный контроль
- Можно использовать PM2 для постоянной работы
- Гибкая настройка

**Шаги:**

1. Установите Node.js на сервер
2. Клонируйте проект
3. Настройте переменные окружения
4. Соберите проект: `npm run build`
5. Запустите с PM2:
```bash
npm install -g pm2
pm2 start npm --name "booking-app" -- start
pm2 save
pm2 startup
```

6. Настройте cron для напоминаний:
```bash
crontab -e
# Добавьте строку:
*/15 * * * * curl -X POST http://localhost:3000/api/reminders/send
```

---

## ⚠️ Важные моменты

### 1. База данных

**Для продакшена НЕ используйте SQLite!**

Рекомендуемые варианты:
- **PostgreSQL** (лучший выбор)
- **MySQL**
- **MongoDB**

**Настройка PostgreSQL:**
1. Создайте базу данных на хостинге
2. Обновите `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```
3. Обновите `.env`:
```
DATABASE_URL="postgresql://user:password@host:5432/dbname"
```

### 2. Webhook для отмены записей

**Требуется публичный HTTPS URL!**

- На Vercel: автоматически работает
- На обычном хостинге: нужен SSL сертификат
- Локально: используйте ngrok для тестирования

**Настройка webhook в Green API:**
1. Получите публичный URL вашего сайта
2. Перейдите в Green API → Настройки → Webhook URL
3. Вставьте: `https://ваш-домен.com/api/whatsapp/webhook`

### 3. Автоматическая отправка напоминаний

**Текущая реализация:**
- Напоминания отправляются при загрузке админ-панели
- Это не надежно для продакшена!

**Решение:**
- Используйте cron jobs (см. варианты выше)
- Или внешний сервис типа EasyCron, Cron-job.org

---

## 🔧 Настройка для продакшена

### 1. Обновите базу данных

Замените SQLite на PostgreSQL в `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

### 2. Миграция базы данных

```bash
npx prisma migrate dev --name init
```

### 3. Переменные окружения

Создайте `.env.production`:
```
DATABASE_URL="postgresql://..."
NODE_ENV="production"
```

---

## 📝 Чеклист перед запуском

- [ ] База данных настроена (не SQLite для продакшена)
- [ ] Переменные окружения установлены
- [ ] Webhook URL настроен в Green API
- [ ] Cron job настроен для автоматических напоминаний
- [ ] SSL сертификат установлен (для HTTPS)
- [ ] Тестирование отправки сообщений
- [ ] Тестирование webhook для отмены записей

---

## 🆘 Проблемы и решения

### Напоминания не отправляются автоматически

**Решение:** Настройте cron job (см. варианты выше)

### Webhook не работает

**Проблема:** Green API не может достучаться до вашего сервера

**Решение:**
- Убедитесь, что URL публичный (не localhost)
- Проверьте, что используется HTTPS
- Проверьте настройки firewall

### База данных не работает

**Проблема:** SQLite не подходит для продакшена

**Решение:** Перейдите на PostgreSQL или другую БД

