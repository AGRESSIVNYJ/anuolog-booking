# Настройка домена для проекта

## 🌐 Если у вас есть домен

Отлично! Домен нужен для:
- ✅ Публичного доступа к сайту
- ✅ HTTPS (обязательно для webhook WhatsApp)
- ✅ Профессионального вида

---

## 📋 Варианты настройки

### Вариант 1: Vercel (Самый простой)

**Преимущества:**
- Бесплатный SSL сертификат
- Автоматическая настройка
- CDN из коробки

**Шаги:**

1. **Разверните проект на Vercel:**
```bash
vercel
```

2. **В Vercel Dashboard:**
   - Перейдите в Settings → Domains
   - Добавьте ваш домен (например: `booking.yourdomain.com`)
   - Vercel покажет DNS записи, которые нужно добавить

3. **Настройте DNS у регистратора домена:**

   **Вариант A: Поддомен (booking.yourdomain.com)**
   ```
   Тип: CNAME
   Имя: booking
   Значение: cname.vercel-dns.com
   ```

   **Вариант B: Основной домен (yourdomain.com)**
   ```
   Тип: A
   Имя: @
   Значение: 76.76.21.21
   
   Тип: CNAME
   Имя: www
   Значение: cname.vercel-dns.com
   ```

4. **Подождите 24-48 часов** для распространения DNS

5. **Vercel автоматически установит SSL сертификат**

---

### Вариант 2: Обычный хостинг с доменом

**Шаги:**

1. **В панели управления хостингом:**
   - Добавьте домен в список доменов
   - Укажите корневую папку (например: `/public_html` или `/www`)

2. **Настройте DNS записи:**
   ```
   Тип: A
   Имя: @
   Значение: IP адрес вашего хостинга
   
   Тип: CNAME
   Имя: www
   Значение: yourdomain.com
   ```

3. **Установите SSL сертификат:**
   - Используйте Let's Encrypt (бесплатно)
   - Или купите SSL сертификат
   - Обычно есть в панели хостинга (cPanel, Plesk и т.д.)

4. **Настройте Next.js для работы с доменом:**
   - Убедитесь, что хостинг поддерживает Node.js
   - Настройте переменные окружения

---

### Вариант 3: VPS/Сервер с доменом

**Шаги:**

1. **Настройте DNS записи:**
   ```
   Тип: A
   Имя: @
   Значение: IP адрес вашего VPS
   
   Тип: A
   Имя: www
   Значение: IP адрес вашего VPS
   ```

2. **Настройте Nginx или Apache:**
   
   **Nginx конфигурация:**
   ```nginx
   server {
       listen 80;
       server_name yourdomain.com www.yourdomain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

3. **Установите SSL через Certbot:**
   ```bash
   sudo apt-get update
   sudo apt-get install certbot python3-certbot-nginx
   sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
   ```

4. **Автоматическое обновление SSL:**
   ```bash
   sudo certbot renew --dry-run
   ```

---

## 🔧 Настройка для WhatsApp Webhook

После настройки домена с HTTPS:

1. **Получите полный URL:**
   ```
   https://yourdomain.com/api/whatsapp/webhook
   ```

2. **В Green API Dashboard:**
   - Перейдите в Настройки → Webhook URL
   - Вставьте: `https://yourdomain.com/api/whatsapp/webhook`
   - Сохраните

3. **Проверьте работу:**
   - Откройте в браузере: `https://yourdomain.com/api/whatsapp/webhook`
   - Должен вернуться: `{"status":"ok","message":"Webhook is active"}`

---

## ⚙️ Настройка переменных окружения

После настройки домена обновите `.env`:

```env
DATABASE_URL="postgresql://user:password@host:5432/dbname"
NODE_ENV="production"
NEXT_PUBLIC_SITE_URL="https://yourdomain.com"
```

---

## 🔒 Важно для HTTPS

### Почему HTTPS обязателен:

1. **WhatsApp Webhook требует HTTPS** - Green API не будет работать с HTTP
2. **Безопасность** - защита данных клиентов
3. **SEO** - Google предпочитает HTTPS сайты
4. **Браузеры** - современные браузеры блокируют HTTP

### Бесплатные SSL сертификаты:

- **Let's Encrypt** - самый популярный, бесплатный
- **Cloudflare** - бесплатный SSL + CDN
- **Vercel** - автоматический SSL

---

## 📝 Чеклист настройки домена

- [ ] Домен добавлен в хостинг/Vercel
- [ ] DNS записи настроены у регистратора
- [ ] Подождали распространение DNS (24-48 часов)
- [ ] SSL сертификат установлен и работает
- [ ] Сайт открывается по HTTPS
- [ ] Webhook URL обновлен в Green API
- [ ] Webhook тестируется и работает
- [ ] Переменные окружения обновлены

---

## 🧪 Проверка настройки

### 1. Проверка DNS:
```bash
# Проверьте, что домен указывает на правильный IP
nslookup yourdomain.com
# или
dig yourdomain.com
```

### 2. Проверка SSL:
- Откройте `https://yourdomain.com` в браузере
- Должна быть зеленая иконка замка
- Проверьте через: https://www.ssllabs.com/ssltest/

### 3. Проверка Webhook:
```bash
curl https://yourdomain.com/api/whatsapp/webhook
# Должен вернуть: {"status":"ok","message":"Webhook is active"}
```

---

## 🆘 Проблемы и решения

### Домен не работает

**Проблема:** DNS еще не распространились

**Решение:** Подождите 24-48 часов, проверьте DNS записи

### SSL не работает

**Проблема:** Сертификат не установлен

**Решение:** 
- Для Vercel: автоматически
- Для хостинга: используйте Let's Encrypt
- Для VPS: установите через Certbot

### Webhook не работает

**Проблема:** Green API не может достучаться

**Решение:**
- Убедитесь, что используется HTTPS
- Проверьте, что URL публичный (не localhost)
- Проверьте настройки firewall

### Сайт не открывается

**Проблема:** Неправильная настройка сервера

**Решение:**
- Проверьте, что приложение запущено
- Проверьте настройки Nginx/Apache
- Проверьте логи сервера

---

## 💡 Рекомендации

1. **Используйте поддомен** для тестирования:
   - `booking.yourdomain.com` - для продакшена
   - `test.yourdomain.com` - для тестирования

2. **Настройте редирект с www:**
   - `www.yourdomain.com` → `yourdomain.com`
   - Или наоборот (выберите один вариант)

3. **Используйте CDN** для ускорения:
   - Cloudflare (бесплатный)
   - Vercel (встроенный)

4. **Регулярно обновляйте SSL:**
   - Let's Encrypt обновляется автоматически
   - Проверяйте срок действия

---

## 📞 Примеры DNS записей

### Для Vercel:
```
Тип: CNAME
Имя: booking
Значение: cname.vercel-dns.com
TTL: 3600
```

### Для обычного хостинга:
```
Тип: A
Имя: @
Значение: 192.168.1.1 (IP вашего хостинга)
TTL: 3600

Тип: CNAME
Имя: www
Значение: yourdomain.com
TTL: 3600
```

### Для VPS:
```
Тип: A
Имя: @
Значение: 123.45.67.89 (IP вашего VPS)
TTL: 3600

Тип: A
Имя: www
Значение: 123.45.67.89 (IP вашего VPS)
TTL: 3600
```


