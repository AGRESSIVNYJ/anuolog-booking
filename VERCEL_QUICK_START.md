# Быстрый старт на Vercel - Пошаговая инструкция

## 🚀 Что делать сейчас (по скриншоту):

### Вариант 1: Импорт через Git (Рекомендуется)

1. **Подготовьте проект на GitHub/GitLab/Bitbucket:**
   - Если еще нет репозитория, создайте его
   - Загрузите ваш код в репозиторий

2. **В Vercel Dashboard (где вы сейчас):**
   - Нажмите на **"Import Project"** (иконка с плюсом в круге)
   - Выберите ваш Git провайдер (GitHub, GitLab, Bitbucket)
   - Разрешите доступ к репозиториям
   - Выберите ваш репозиторий с проектом

3. **Настройте проект:**
   - Vercel автоматически определит Next.js
   - Проверьте настройки:
     - **Framework Preset:** Next.js (должно быть автоматически)
     - **Root Directory:** `./` (если проект в корне)
     - **Build Command:** `npm run build` (автоматически)
     - **Output Directory:** `.next` (автоматически)

4. **Добавьте переменные окружения:**
   - Нажмите "Environment Variables"
   - Добавьте:
     ```
     DATABASE_URL=postgresql://user:password@host:5432/dbname
     NODE_ENV=production
     ```
   - ⚠️ **Важно:** Для продакшена используйте PostgreSQL, не SQLite!

5. **Нажмите "Deploy"**

6. **Готово!** Ваш сайт будет доступен по адресу:
   - `ваш-проект.vercel.app`
   - Или ваш домен, если подключите

---

### Вариант 2: Через Vercel CLI (Альтернатива)

Если хотите развернуть через командную строку:

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите:**
   ```bash
   vercel login
   ```

3. **В папке вашего проекта:**
   ```bash
   vercel
   ```

4. **Следуйте инструкциям:**
   - Выберите проект или создайте новый
   - Подтвердите настройки
   - Vercel автоматически развернет проект

---

## ⚙️ После развертывания:

### 1. Настройте базу данных:

**Важно:** SQLite не работает на Vercel! Нужна внешняя БД.

**Варианты:**
- **Vercel Postgres** (встроенный, платный от $20/мес)
- **Supabase** (бесплатный до 500MB)
- **Railway** (бесплатный план)
- **Neon** (бесплатный план)
- **Любой другой PostgreSQL хостинг**

**Настройка Supabase (бесплатно):**
1. Зарегистрируйтесь на https://supabase.com
2. Создайте проект
3. Скопируйте Connection String
4. Обновите `prisma/schema.prisma`:
   ```prisma
   datasource db {
     provider = "postgresql"
     url      = env("DATABASE_URL")
   }
   ```
5. Добавьте `DATABASE_URL` в Vercel Environment Variables

### 2. Примените миграции базы данных:

После настройки БД:
```bash
# Локально или через Vercel CLI
npx prisma migrate deploy
# или
npx prisma db push
```

### 3. Настройте Cron Jobs для напоминаний:

Файл `vercel.json` уже создан в проекте. Vercel автоматически настроит cron jobs.

Проверьте в Vercel Dashboard:
- Settings → Cron Jobs
- Должен быть: `/api/reminders/send` каждые 15 минут

### 4. Настройте домен (если есть):

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи (см. DOMAIN_SETUP.md)

### 5. Настройте WhatsApp Webhook:

1. Получите URL вашего сайта:
   - `https://ваш-проект.vercel.app/api/whatsapp/webhook`
   - Или `https://ваш-домен.com/api/whatsapp/webhook`

2. В Green API Dashboard:
   - Настройки → Webhook URL
   - Вставьте URL выше
   - Сохраните

---

## ✅ Чеклист развертывания:

- [ ] Проект загружен в Git репозиторий
- [ ] Импортирован в Vercel через "Import Project"
- [ ] Настроена база данных PostgreSQL
- [ ] Добавлены Environment Variables в Vercel
- [ ] Применены миграции базы данных
- [ ] Проект успешно развернут
- [ ] Cron Jobs настроены (автоматически через vercel.json)
- [ ] Домен подключен (если есть)
- [ ] WhatsApp Webhook настроен
- [ ] Протестирована отправка сообщений

---

## 🆘 Проблемы и решения:

### Ошибка при сборке:

**Проблема:** `Error: Prisma schema validation`

**Решение:**
- Убедитесь, что `DATABASE_URL` добавлен в Environment Variables
- Проверьте, что используется PostgreSQL, не SQLite

### База данных не работает:

**Проблема:** SQLite не поддерживается на Vercel

**Решение:**
- Перейдите на PostgreSQL (Supabase, Railway, Neon)
- Обновите `prisma/schema.prisma`
- Примените миграции

### Напоминания не отправляются:

**Проблема:** Cron Jobs не работают

**Решение:**
- Проверьте файл `vercel.json` в проекте
- Убедитесь, что он загружен в Git
- Проверьте Settings → Cron Jobs в Vercel

---

## 📝 Следующие шаги:

1. **Сейчас:** Нажмите "Import Project" в Vercel
2. **Выберите:** Ваш Git репозиторий
3. **Настройте:** Environment Variables
4. **Разверните:** Нажмите "Deploy"
5. **Настройте:** Базу данных PostgreSQL
6. **Подключите:** Домен (если есть)
7. **Настройте:** WhatsApp Webhook

**Готово! Ваш сайт будет работать бесплатно на Vercel!** 🎉


