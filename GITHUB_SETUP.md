# 📦 Создание репозитория на GitHub и загрузка проекта

## 🚀 Пошаговая инструкция:

### Шаг 1: Создайте репозиторий на GitHub

1. **Зайдите на GitHub:**
   - Откройте https://github.com
   - Войдите в свой аккаунт (AGRESSIVNYJ)

2. **Создайте новый репозиторий:**
   - Нажмите на **"+"** в правом верхнем углу
   - Выберите **"New repository"**
   - Или перейдите по прямой ссылке: https://github.com/new

3. **Заполните форму:**
   - **Repository name:** `manuolog-booking` (или любое другое имя)
   - **Description:** "Сайт для записи на сеанс мануальной терапии"
   - **Visibility:** 
     - ✅ **Public** (если хотите бесплатный хостинг)
     - ⚠️ **Private** (если нужна приватность, но потребуется платный план Vercel)
   - **НЕ ставьте галочки:**
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - (Мы уже создали эти файлы в проекте)

4. **Нажмите "Create repository"**

---

### Шаг 2: Загрузите код в репозиторий

После создания репозитория GitHub покажет инструкции. Выберите один из способов:

---

#### **Способ 1: Через командную строку (рекомендуется)**

Откройте PowerShell в папке проекта (`D:\sitework`) и выполните:

```powershell
# Инициализируйте Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: сайт для записи на мануальную терапию"

# Добавьте удаленный репозиторий (замените AGRESSIVNYJ и manuolog-booking на ваши)
git remote add origin https://github.com/AGRESSIVNYJ/manuolog-booking.git

# Загрузите код
git branch -M main
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Используйте Personal Access Token (см. ниже)

---

#### **Способ 2: Через GitHub Desktop (проще для новичков)**

1. **Скачайте GitHub Desktop:**
   - https://desktop.github.com
   - Установите и войдите

2. **Добавьте репозиторий:**
   - File → Add Local Repository
   - Выберите папку `D:\sitework`
   - Если Git еще не инициализирован, GitHub Desktop предложит это сделать

3. **Закоммитьте и загрузите:**
   - Напишите сообщение коммита: "Initial commit"
   - Нажмите "Commit to main"
   - Нажмите "Publish repository"
   - Выберите ваш репозиторий на GitHub

---

#### **Способ 3: Через VS Code (если используете)**

1. **Откройте проект в VS Code:**
   - File → Open Folder → выберите `D:\sitework`

2. **Инициализируйте Git:**
   - Нажмите на иконку Source Control (слева)
   - Нажмите "Initialize Repository"

3. **Закоммитьте:**
   - Нажмите "+" рядом с "Changes"
   - Введите сообщение: "Initial commit"
   - Нажмите "Commit"

4. **Опубликуйте:**
   - Нажмите "Publish Branch"
   - Выберите ваш репозиторий на GitHub

---

### Шаг 3: Проверьте загрузку

1. Обновите страницу репозитория на GitHub
2. Вы должны увидеть все файлы проекта:
   - `app/`
   - `prisma/`
   - `package.json`
   - `README.md`
   - и т.д.

---

## 🔐 Если нужен Personal Access Token (для способа 1):

GitHub больше не принимает пароли при push через HTTPS. Нужен токен:

1. **Создайте токен:**
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Или прямая ссылка: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Название: `Vercel Deployment`
   - Выберите срок действия (например, 90 дней)
   - Отметьте галочку: **`repo`** (полный доступ к репозиториям)
   - Нажмите "Generate token"
   - **Скопируйте токен** (показывается только один раз!)

2. **Используйте токен вместо пароля:**
   - При `git push` GitHub попросит:
     - Username: `AGRESSIVNYJ`
     - Password: **вставьте токен** (не пароль!)

---

## ✅ Что должно быть в репозитории:

Проверьте, что загружены все важные файлы:

- ✅ `package.json`
- ✅ `prisma/schema.prisma`
- ✅ `app/` (вся папка)
- ✅ `lib/` (если есть)
- ✅ `vercel.json`
- ✅ `.gitignore` (чтобы не загружать `node_modules` и `.env`)
- ✅ `README.md`

**НЕ должны быть загружены:**
- ❌ `node_modules/` (должен быть в `.gitignore`)
- ❌ `.env` (должен быть в `.gitignore`)
- ❌ `prisma/dev.db` (локальная БД, не нужна)

---

## 🎯 Следующий шаг:

После загрузки кода на GitHub:

1. **Разверните на Vercel:**
   - Зайдите на https://vercel.com
   - Нажмите "Import Project"
   - Выберите ваш репозиторий
   - Следуйте инструкциям (см. `VERCEL_QUICK_START.md`)

---

## 🆘 Проблемы и решения:

### Ошибка: "Authentication failed"

**Решение:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

### Ошибка: "Repository not found"

**Решение:**
- Проверьте, что репозиторий создан
- Проверьте правильность URL: `https://github.com/AGRESSIVNYJ/название-репозитория`

### Файлы не загружаются

**Решение:**
- Убедитесь, что выполнили `git add .`
- Проверьте `.gitignore` - возможно, файлы игнорируются

---

**Готово! После загрузки кода можно разворачивать на Vercel!** 🎉
